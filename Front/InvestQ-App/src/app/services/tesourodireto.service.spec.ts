/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { TesourodiretoService } from './tesourodireto.service';

describe('Service: Tesourodireto', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [TesourodiretoService]
    });
  });

  it('should ...', inject([TesourodiretoService], (service: TesourodiretoService) => {
    expect(service).toBeTruthy();
  }));
});
