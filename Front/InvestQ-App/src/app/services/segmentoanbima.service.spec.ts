/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { SegmentoanbimaService } from './segmentoanbima.service';

describe('Service: Segmentoanbima', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [SegmentoanbimaService]
    });
  });

  it('should ...', inject([SegmentoanbimaService], (service: SegmentoanbimaService) => {
    expect(service).toBeTruthy();
  }));
});
