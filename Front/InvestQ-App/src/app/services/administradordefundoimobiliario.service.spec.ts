/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AdministradordefundoimobiliarioService } from './administradordefundoimobiliario.service';

describe('Service: Adminstradordefundoimobiliario', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AdministradordefundoimobiliarioService]
    });
  });

  it('should ...', inject([AdministradordefundoimobiliarioService], (service: AdministradordefundoimobiliarioService) => {
    expect(service).toBeTruthy();
  }));
});
